// Packages
import styled from "styled-components";
import tw from "twin.macro";

export const StyledHeader = styled.header.attrs({
  className: "StyledHeader bg-ilusion-header"
})`
  ${tw`h-14 w-full py-3 px-6`}
`